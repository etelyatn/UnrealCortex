{
  "core": "Route UnrealCortex core commands through `core_cmd(command, params)`.\nAvailable commands:\n- get_status()\n- save_asset(asset_path: string, only_if_is_dirty: boolean = optional)",
  "data": "Route UnrealCortex data commands through `data_cmd(command, params)`.\nAvailable commands:\n- query_datatable(table_path: string, row_filter: string = optional)",
  "blueprint": "Route UnrealCortex blueprint commands through `blueprint_cmd(command, params)`.",
  "graph": "Route UnrealCortex graph commands through `graph_cmd(command, params)`.",
  "level": "Route UnrealCortex level commands through `level_cmd(command, params)`.",
  "material": "Route UnrealCortex material commands through `material_cmd(command, params)`.",
  "umg": "Route UnrealCortex umg commands through `umg_cmd(command, params)`.",
  "qa": "Route UnrealCortex qa commands through `qa_cmd(command, params)`.",
  "reflect": "Route UnrealCortex reflect commands through `reflect_cmd(command, params)`.",
  "editor": "Route UnrealCortex editor commands through `editor_cmd(command, params)`."
}
